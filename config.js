// You need to provide your own values for these two settings.
// See the comments at the top of index.html.
const APP_ROOT = 'https://SPADemo--msfapi.repl.co';
const CLIENT_ID = 'test-demo-spa';

// Calculate frequently-used URLs from the above:
const APP_HOME = `${APP_ROOT}/demo.html`;
const APP_CALLBACK = `${APP_ROOT}/redirect.html`;
